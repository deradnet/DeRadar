<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude cache directory from backups for better performance -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="cache" path="." />
    <exclude domain="external" path="." />
</full-backup-content>
